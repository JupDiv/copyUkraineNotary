import styled from 'styled-components';
import Image from 'next/image';

export const AboutImageItem = styled(Image)`
  width: 300px;
  height: 300px;
`;

export const AboutImageContainer = styled.div`
  display: flex;
  align-items: center;
}`;
